package net.server.tcp.serverCommands;

import java.io.*;

public class CommandServerUpdate extends CommandServer {

    public CommandServerUpdate(DataInputStream in, DataOutputStream out) {
        super(in, out);
    }

    public void run() throws IOException {

    }
}
