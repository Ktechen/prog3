package controller.event.events.event.show;

import controller.event.Event;

public class EventShowAll extends Event {
    public EventShowAll(Object source, String text) {
        super(source, text);
    }
}
