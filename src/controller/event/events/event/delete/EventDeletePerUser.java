package controller.event.events.event.delete;

import controller.event.Event;

public class EventDeletePerUser extends Event {
    public EventDeletePerUser(Object source, String text) {
        super(source, text);
    }
}
