package controller.event.events.event.delete;

import controller.event.Event;

public class EventDeletePerAddress extends Event {
    public EventDeletePerAddress(Object source, String text) {
        super(source, text);
    }
}
