package controller.event.events.event.add;

import controller.event.Event;

public class EventAddMediaFiles extends Event {
    public EventAddMediaFiles(Object source, Object[] arr, String type) {
        super(source, arr, type);
    }
}
