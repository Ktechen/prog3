package controller.event.events.event.update;

import controller.event.Event;

public class EventUpdateCounter extends Event {
    public EventUpdateCounter(Object source, String text) {
        super(source, text);
    }
}
